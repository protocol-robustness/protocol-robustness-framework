{:suite/id "sew-domain-reference-v1"
 :suite/version "1.0.0"
 :config/version "reference-config-v1"

 ;; Sew batch conflict domain scenarios (5). All are simulator-backed.
 :scenarios
 [{:id "same-block-dual-resolution-v1"
   :ordinal 1
   :trace-slug "sew-001-same-block-dual-resolution"
   :claim-id "settlement-mutual-exclusion"
   :claim "Settlement paths remain mutually exclusive under concurrent ordering."
   :threat "Ordering-dependent double settlement"
   :primary-threat "ordering-dependent-double-settlement"
   :classification :safety-critical
   :invariant-ids ["no-double-settlement"]
   :expectations-passed 2
   :invariants-passed 1
   :simulator/scenario-path "scenarios/edn/S56_same-block-dual-resolution-order-invariant.edn"
   :upgrade-path "maintain deterministic replay and trace hash stability"}

  {:id "pending-settlement-expiry-v1"
   :ordinal 2
   :trace-slug "sew-002-pending-settlement-expiry"
   :claim-id "pending-settlement-boundary-enforced"
   :claim "Pending settlement expiry is enforced at boundary conditions."
   :threat "Settlement window manipulation"
   :primary-threat "settlement-window-manipulation"
   :classification :boundary-safety
   :invariant-ids ["no-stale-pending-settlements"]
   :expectations-passed 2
   :invariants-passed 1
   :simulator/scenario-path "scenarios/edn/S57_pending-settlement-expiry-boundary-1s.edn"
   :upgrade-path "maintain deterministic replay and trace hash stability"}

  {:id "escalation-after-terminal-v1"
   :ordinal 3
   :trace-slug "sew-003-escalation-after-terminal"
   :claim-id "terminal-state-irreversible-after-escalation"
   :claim "Escalation after terminal state is rejected."
   :threat "Post-terminal escalation"
   :primary-threat "post-terminal-escalation"
   :classification :safety-critical
   :invariant-ids ["terminal-states-unchanged"]
   :expectations-passed 2
   :invariants-passed 1
   :simulator/scenario-path "scenarios/edn/S58_escalation-after-terminal-hard-reject.edn"
   :upgrade-path "maintain deterministic replay and trace hash stability"}

  {:id "force-refund-illegal-release-v1"
   :ordinal 4
   :trace-slug "sew-004-force-refund-illegal-release"
   :claim-id "terminal-state-absorbing-after-force-refund"
   :claim "Force-refunded escrows reject subsequent release attempts."
   :threat "Post-terminal settlement bypass"
   :primary-threat "post-terminal-settlement-bypass"
   :classification :safety-critical
   :invariant-ids ["terminal-states-unchanged" "single-resolution-payout-consistent"]
   :expectations-passed 2
   :invariants-passed 2
   :simulator/scenario-path "scenarios/edn/S64_force-refund-then-illegal-release-attempt.edn"
   :upgrade-path "maintain deterministic replay and trace hash stability"}

  {:id "escalation-supersedes-pending-v1"
   :ordinal 5
   :trace-slug "sew-005-escalation-supersedes-pending"
   :claim-id "escalation-supersedes-pending-settlement"
   :claim "Escalation supersedes pending settlement and archives it."
   :threat "Pending settlement escalation bypass"
   :primary-threat "pending-settlement-escalation-bypass"
   :classification :safety-critical
   :invariant-ids ["pending-settlement-consistent" "escalation-level-monotonic"]
   :expectations-passed 2
   :invariants-passed 2
   :simulator/scenario-path "scenarios/edn/S65_escalation-supersedes-old-pending-then-old-path-attempt.edn"
   :upgrade-path "maintain deterministic replay and trace hash stability"}]

 :invariants
 [{:id "no-double-settlement" :scenarios ["same-block-dual-resolution-v1"]}
  {:id "no-stale-pending-settlements" :scenarios ["pending-settlement-expiry-v1"]}
  {:id "terminal-states-unchanged" :scenarios ["escalation-after-terminal-v1" "force-refund-illegal-release-v1"]}
  {:id "single-resolution-payout-consistent" :scenarios ["force-refund-illegal-release-v1"]}
  {:id "pending-settlement-consistent" :scenarios ["escalation-supersedes-pending-v1"]}
  {:id "escalation-level-monotonic" :scenarios ["escalation-supersedes-pending-v1"]}]

 :claims
 [{:claim-id :settlement-mutual-exclusion
   :claim "Settlement paths remain mutually exclusive under concurrent ordering."
   :threat "Ordering-dependent double settlement"
   :invariant-ids ["no-double-settlement"]
   :scenario-ids ["same-block-dual-resolution-v1"]
   :upgrade-path "maintain deterministic replay and trace hash stability"}
  {:claim-id :pending-settlement-boundary-enforced
   :claim "Pending settlement expiry is enforced at boundary conditions."
   :threat "Settlement window manipulation"
   :invariant-ids ["no-stale-pending-settlements"]
   :scenario-ids ["pending-settlement-expiry-v1"]
   :upgrade-path "maintain deterministic replay and trace hash stability"}
  {:claim-id :terminal-state-irreversible-after-escalation
   :claim "Escalation after terminal state is rejected."
   :threat "Post-terminal escalation"
   :invariant-ids ["terminal-states-unchanged"]
   :scenario-ids ["escalation-after-terminal-v1"]
   :upgrade-path "maintain deterministic replay and trace hash stability"}
  {:claim-id :terminal-state-absorbing-after-force-refund
   :claim "Force-refunded escrows reject subsequent release attempts."
   :threat "Post-terminal settlement bypass"
   :invariant-ids ["terminal-states-unchanged" "single-resolution-payout-consistent"]
   :scenario-ids ["force-refund-illegal-release-v1"]
   :upgrade-path "maintain deterministic replay and trace hash stability"}
  {:claim-id :escalation-supersedes-pending-settlement
   :claim "Escalation supersedes pending settlement and archives it."
   :threat "Pending settlement escalation bypass"
   :invariant-ids ["pending-settlement-consistent" "escalation-level-monotonic"]
   :scenario-ids ["escalation-supersedes-pending-v1"]
   :upgrade-path "maintain deterministic replay and trace hash stability"}]

 :economic-checks
 [{:check-id "same-block-dual-resolution-economic-baseline"
   :classification :safety-critical
   :scenario-id "same-block-dual-resolution-v1"}
  {:check-id "escalation-after-terminal-economic-baseline"
   :classification :safety-critical
   :scenario-id "escalation-after-terminal-v1"}]}
