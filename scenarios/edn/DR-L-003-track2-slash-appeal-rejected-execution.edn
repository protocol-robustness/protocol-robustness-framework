{:scenario-id "dr-l-003-track2-slash-appeal-rejected-execution"
 :schema-version "1.0"
 :allow-open-disputes? true
 :purpose "adversarial-robustness"
 :title "Track 2 reversal slash: appeal rejected, bond forfeited, then slash executed"
 :description "Track 2 pending reversal slash created via submit-evidence + reversal. Resolver appeals within window (posts bond). Governance rejects the appeal: bond forfeited to insurance pool, slash returns to pending. After original appeal deadline, execute-fraud-slash executes the slash."
 :provenance {:author "@dispute-researcher" :created-at "2026-06-30" :license "CC-BY-4.0"}
 :claim {:id "track2-slash-appeal-rejected-executes"
         :statement "When a resolver appeals a Track 2 reversal slash and governance rejects, the appeal bond is forfeited and the slash returns to :pending. execute-fraud-slash can then execute after the appeal deadline."
         :falsifies-if ["Appeal rejection does not return slash to :pending"
                        "execute-fraud-slash fails after appeal rejection"
                        "Appeal bond is refunded despite rejection"
                        "Resolver stake not deducted after execution"]}
:expected-failures {:dr-l-003-track2-slash-appeal-rejected-execution [:no-stale-automatable-escrows]}
:expectations {:terminal-state "refunded"
               :metrics {:slash_count {:eq 1}
                         :resolver_stake_deducted {:gt 0}}}
 :threat {:family "resolution-slashing" :attack "track2-appeal-rejected-then-executed" :severity "high"
          :actors ["resolver"] :victims ["protocol"]
          :assumptions ["Governance can reject a slash appeal"
                       "Bond is forfeited on rejection"
                       "Rejected slash returns to pending for execution"]}
 :accounting {:unit "USDC" :decimals 6 :tracked-balances ["buyer" "seller" "resolver" "governance" "protocol" "insurance" "slashPool"]}
 :initial-block-time 1000
 :protocol-params {:governance-mode :legacy, :resolver-fee-bps 150 :appeal-window-duration 120 :max-dispute-duration 2592000
                   :reversal-slash-bps 2500 :appeal-bond-bps 500
                   :dispute-resolver "0xresolver-l0"
                   :escalation-resolvers {:1 "0xresolver-l1"}}
 :agents [{:id "buyer" :address "0xbuyer" :strategy "honest"}
          {:id "seller" :address "0xseller" :strategy "honest"}
          {:id "resolver-l0" :address "0xresolver-l0" :role "resolver" :stake 20000}
          {:id "resolver-l1" :address "0xresolver-l1" :role "resolver" :stake 20000}
          {:id "governance" :address "0xgov" :role "governance"}
          {:id "keeper" :address "0xkeeper" :role "keeper"}]
 :events [{:seq 0 :time 1000 :agent "resolver-l0" :action "register_stake"
           :params {:amount 20000}}
          {:seq 1 :time 1000 :agent "resolver-l1" :action "register_stake"
           :params {:amount 20000}}
          {:seq 2 :time 1000 :agent "buyer" :action "create_escrow"
           :params {:token "USDC" :to "0xseller" :amount 5000}}
          {:seq 3 :time 1060 :agent "seller" :action "raise_dispute" :params {:workflow-id 0}}
          ;; L0 resolves: release
          {:seq 4 :time 1120 :agent "resolver-l0" :action "execute_resolution"
           :params {:workflow-id 0 :is-release true :resolution-hash "0xrelease"}}
          ;; New evidence for Track 2
          {:seq 5 :time 1150 :agent "seller" :action "submit_evidence"
           :params {:workflow-id 0 :evidence-hash "0xnew-evidence"}}
          ;; Escalate to L1 before appeal deadline
          {:seq 6 :time 1160 :agent "seller" :action "escalate_dispute"
           :params {:workflow-id 0}}
          ;; L1 reverses L0: creates pending Track 2 slash (deadline = 1200+120 = 1320)
          ;; Final round → escrow finalized (refunded) immediately
          {:seq 7 :time 1200 :agent "resolver-l1" :action "execute_resolution"
           :params {:workflow-id 0 :is-release false :resolution-hash "0xreversal"}}
          ;; Resolver appeals the pending reversal slash (1310 < deadline 1320)
          {:seq 8 :time 1310 :agent "resolver-l0" :action "appeal_slash"
           :params {:workflow-id 0 :slash-id {:slash-ref {:workflow-id 0 :kind :reversal :level 0}}}}
          ;; Governance rejects the appeal: bond forfeited, slash returns to pending
          {:seq 9 :time 1400 :agent "governance" :action "resolve_appeal"
           :params {:workflow-id 0 :slash-id {:slash-ref {:workflow-id 0 :kind :reversal :level 0}} :upheld? false}}
          ;; Past original deadline (1320). Execute the pending slash.
          {:seq 10 :time 1450 :agent "keeper" :action "execute_fraud_slash"
                     :params {:workflow-id 0 :slash-id {:slash-ref {:workflow-id 0 :kind :reversal :level 0}}}}]}
