{:benchmark/id              :benchmark/prf-redistribution-fairness-v0
 :benchmark/version         0
 :benchmark/status          :experimental
 :benchmark/coverage-status :partial
 :benchmark/experimental-reason
 "The three redistribution-fairness claims
  (claim/level-cascade-integrity, claim/epoch-bridge-fairness,
  claim/pool-boundary-respected) are registered in the claim registry but
  have no evaluator implementation yet. They are NOT per-scenario claims,
  so :benchmark/deferred-scenario-claims is correctly empty — the deferred
  claims are benchmark-level. This pack exercises the scenarios and
  validates mechanical claims only."
 :benchmark/domain          :domain/protocol-value-conservation
 :benchmark/protocol        :protocol/prf
 :benchmark/scenario-suite  :suite/prf-redistribution-fairness-v0
 :benchmark/scenario-suite-description
 "Eight scenarios testing redistribution fairness across three dimensions:
  cross-level cascade (surplus flows between priority levels), inter-temporal
  bridge (deferred claim recovery across epochs), and cross-pool boundary
  enforcement (pool segregation during shortage)."
 :benchmark/runner-policy   :runner-policy/deterministic-local-v1
 :benchmark/evidence-policy :evidence-policy/forensic-standard-v1
 :benchmark/scoring-rule    :scoring/binary-claims-v1
 :benchmark/property-types  #{:fairness :safety}
 :benchmark/description
 "Experimental redistribution fairness research profile across eight
  scenarios covering three dimensions: cross-level cascade (surplus flows
  between priority levels), inter-temporal bridge (deferred claim recovery
  across epochs), and cross-pool boundary enforcement (pool segregation
  during shortage). Each scenario is tagged with a redistribution
  dimension. Three redistribution-specific claims are registered in the
  claim registry but deferred — scenario-level mechanical claims
  (evidence-root-present, replay-result-present, no-invariant-errors,
  all-scenarios-pass) are active."
 :benchmark/purpose
 "Evaluate redistribution fairness: whether surplus cascades correctly
  between priority levels, deferred claims are treated fairly across
  epochs, and pool boundaries are respected during shortages."
:benchmark/concepts        [:allocation/redistribution-fairness
                              :allocation/pro-rata-fairness
                              :allocation/partial-fill
                              :allocation/shortfall
                              :allocation/deferred-claim
                              :allocation/slash-obligation
                              :allocation/inter-temporal-bridge
                              :allocation/cross-level-cascade
                              :allocation/cross-pool-boundary
                              :consensus/evidence
                              :consensus/finality
                              :consensus/legitimacy
                              :verifiable-assurance/forensic-confidence]
 :benchmark/scenarios
 [{:scenario/id "S82_shortfall-recovery-cycle"
   :scenario/description "Deferred yield claim from prior epoch is recovered from current surplus; tests epoch-bridge fairness"
   :dimension   :allocation/inter-temporal-bridge
   :claim       :evidence-root-present}
  {:scenario/id "S103_negative-yield-shortfall-cascade"
   :scenario/description "Negative yield triggers shortfall that cascades across yield positions; tests cross-level cascade"
   :dimension   :allocation/cross-level-cascade
   :claim       :evidence-root-present}
  {:scenario/id "S105_policy-response-cascade"
   :scenario/description "Policy response triggers surplus cascade across priority levels after a shortfall event"
   :dimension   :allocation/cross-level-cascade
   :claim       :evidence-root-present}
  {:scenario/id "S108_senior-coverage-consumed-by-slash"
   :scenario/description "Senior coverage consumed by resolver slashing triggers surplus cascade to junior priority level"
   :dimension   :allocation/cross-level-cascade
   :claim       :evidence-root-present}
  {:scenario/id "S114_multi-escrow-pool-shortage"
   :scenario/description "Shortage across multiple escrow pools tests pool-boundary enforcement and segregation"
   :dimension   :allocation/cross-pool-boundary
   :claim       :evidence-root-present}
  {:scenario/id "S-DR-043-payout-shortfall-deferred"
   :scenario/description "Payout shortfall produces deferred remainder; tests inter-temporal deferred claim recording"
   :dimension   :allocation/inter-temporal-bridge
   :claim       :evidence-root-present}
  {:scenario/id "S110_multi-junior-coverage"
   :scenario/description "Multiple junior positions interact with senior coverage; tests cross-level cascade when senior is exhausted"
   :dimension   :allocation/cross-level-cascade
   :claim       :evidence-root-present}
  {:scenario/id "Y04_vault-recovery-claim-deferred"
   :scenario/description "Vault deferred claim recovery across vault cycles; tests epoch-bridge fairness"
   :dimension   :allocation/inter-temporal-bridge
   :claim       :evidence-root-present}]
 :benchmark/deferred-scenario-claims #{}
 :benchmark/claims
 [{:claim/id :evidence-root-present
   :claim/role :primary
   :claim/rationale "Each redistribution scenario must produce verifiable evidence."
   :claim/failure-meaning "Evidence root hash is missing."}
  {:claim/id :replay-result-present
   :claim/role :primary
   :claim/rationale "Replay must confirm that redistribution outcomes are reproducible."
   :claim/failure-meaning "Scenario outcome is missing from replay."}
  {:claim/id :scenario-hash-present
   :claim/role :supporting
   :claim/rationale "Evidence root hash integrity confirms the evidence bundle is unmodified."
   :claim/failure-meaning "Evidence root is not a valid hash."}
  {:claim/id :no-invariant-errors
   :claim/role :diagnostic
   :claim/rationale "Invariant failures may indicate incorrect redistribution logic."
   :claim/failure-meaning "Post-hoc invariants reported failures."}
  {:claim/id :all-scenarios-pass
   :claim/role :primary
   :claim/rationale "All redistribution scenarios must complete to assess fairness."
   :claim/failure-meaning "Some redistribution scenarios failed to complete."}]}
